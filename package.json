{
  "name": "commit-generator-zh-cn",
  "displayName": "commit-generator-zh-cn",
  "description": "在源代码管理中根据 Git 变更自动生成提交信息。",
  "version": "0.0.1",
  "publisher": "zuojinpu",
  "author": "zuojinpu",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jpzuo/commit-generator.git"
  },
  "homepage": "https://github.com/jpzuo/commit-generator#readme",
  "bugs": {
    "url": "https://github.com/jpzuo/commit-generator/issues"
  },
  "keywords": [
    "git",
    "commit",
    "conventional-commits",
    "source-control",
    "openai",
    "chinese"
  ],
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "SCM Providers"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:commitGenerator.generate",
    "workspaceContains:.git"
  ],
  "extensionDependencies": [
    "vscode.git"
  ],
  "main": "./out/extension.js",
  "icon": "media/icon.png",
  "contributes": {
    "commands": [
      {
        "command": "commitGenerator.generate",
        "title": "生成 Commit 信息",
        "category": "提交信息生成器",
        "icon": {
          "light": "media/wand-light.svg",
          "dark": "media/wand-dark.svg"
        }
      }
    ],
    "configuration": {
      "title": "提交信息生成器",
      "properties": {
        "commitGenerator.openaiApiKey": {
          "type": "string",
          "default": "",
          "markdownDescription": "用于生成提交信息的 OpenAI API Key。为空时会使用环境变量 `OPENAI_API_KEY`。"
        },
        "commitGenerator.apiKey": {
          "type": "string",
          "default": "",
          "markdownDescription": "OpenAI 兼容中转服务的 API Key。为空时会依次回退到 `commitGenerator.openaiApiKey` 和 `OPENAI_API_KEY`。"
        },
        "commitGenerator.apiBaseUrl": {
          "type": "string",
          "default": "https://api.openai.com",
          "markdownDescription": "OpenAI 兼容接口的基础地址，例如 `https://api.openai.com` 或你的中转地址。"
        },
        "commitGenerator.apiProtocol": {
          "type": "string",
          "enum": [
            "chatCompletions",
            "responses"
          ],
          "default": "chatCompletions",
          "markdownDescription": "接口协议类型。大多数中转服务使用 `chatCompletions`。"
        },
        "commitGenerator.openaiModel": {
          "type": "string",
          "default": "gpt-4.1-mini",
          "markdownDescription": "用于生成提交信息的 OpenAI 模型。"
        },
        "commitGenerator.chineseStyle": {
          "type": "string",
          "enum": [
            "business",
            "engineering",
            "concise"
          ],
          "default": "engineering",
          "markdownDescription": "中文提交风格模板。`business`：偏业务价值表达；`engineering`：偏技术改动表达；`concise`：简洁直接表达。"
        }
      }
    },
    "menus": {
      "scm/title": [
        {
          "command": "commitGenerator.generate",
          "when": "scmProvider == git",
          "group": "navigation@9"
        }
      ],
      "commandPalette": [
        {
          "command": "commitGenerator.generate"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.7.1",
    "typescript": "^5.5.4"
  }
}
